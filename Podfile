# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Server-Driven-UI' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Server-Driven-UI

  pod 'Marshal'
  pod 'Alamofire'
  pod 'SwiftyJSON', '~> 4.0'

  target 'Server-Driven-UITests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Server-Driven-UIUITests' do
    # Pods for testing
  end

end
